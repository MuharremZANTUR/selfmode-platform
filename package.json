{
  "name": "selfmode-platform",
  "version": "1.0.1",
  "description": "SelfMode - AI-powered career analysis platform",
  "main": "backend/server.js",
  "scripts": {
    "start": "cd backend && npm start",
    "build": "cd frontend && npm install && npm run build",
    "install-deps": "cd backend && npm install && cd ../frontend && npm install",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm start\"",
    "backend": "cd backend && npm start",
    "frontend": "cd frontend && npm start",
    "heroku-postbuild": "cd frontend && npm install && npm run build",
    "render-build": "cd frontend && npm install && npm run build && cd ../backend && npm install",
    "start": "cd backend && npm run prod"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  },
  "keywords": [
    "career",
    "analysis",
    "ai",
    "assessment",
    "selfmode"
  ],
  "author": "SelfMode Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}